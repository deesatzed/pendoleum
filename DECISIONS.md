# Pendoleum Decisions

## 2026-08-03 — Finish-goal scope

- Preserve `index.html` as the playable runtime source and keep the no-build, no-backend design.
- Treat `idea1.md`, the dated architecture packet, and the dated ship plan as historical references; current behavior is defined by `index.html` plus current verification evidence.
- Add development-only verification scripts without introducing a runtime framework or dependency.
- Treat missing MIDI hardware and subjective audio listening as explicitly labeled operator evidence, not as reasons to fake a pass.
- Do not add persistence, accounts, cloud services, new communication/science features, or a legal license during this finish pass.
- Push only a scoped, verified fast-forward commit to `origin/main`; never force-push or deploy.

## 2026-08-03 — Verification and evidence

- Use a no-dependency Node VM harness for deterministic runtime logic and a real Chrome DevTools smoke for browser behavior; neither replaces the other.
- Treat a non-empty MediaRecorder chunk and zero page JavaScript errors as objective browser evidence; keep subjective listening and external MIDI output explicitly operator-dependent.
- Refresh the screenshot from the current expanded UI and make the dated design/plan artifacts explicitly historical instead of rewriting their original record.
- Correct unsupported README claims rather than changing the GA fitness function or inventing a novelty metric.
